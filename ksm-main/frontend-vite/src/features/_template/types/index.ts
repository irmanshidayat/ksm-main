/**
 * Feature Types
 * Types untuk feature ini
 * 
 * Copy types dari frontend CRA:
 * - frontend/src/pages/[feature]/types.ts
 * - frontend/src/types/[feature].ts
 */

// Example types
export interface ExampleType {
  id: number;
  name: string;
  createdAt: string;
}

